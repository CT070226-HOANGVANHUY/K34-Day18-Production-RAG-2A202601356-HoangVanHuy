# cp .env.example .env
# Required for M4 (RAGAS eval) and M5 (Enrichment LLM calls)
OPENAI_API_KEY=sk-...
# OCR scan PDFs is opt-in because it costs one vision call per page.
ENABLE_OCR=false
SKIP_PDF=true
OCR_MODEL=gpt-4o-mini
